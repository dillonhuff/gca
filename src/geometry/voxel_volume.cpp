#include "geometry/voxel_volume.h"

namespace gca {

  voxel_volume::voxel_volume(const point origin,
			     const double x_len,
			     const double y_len,
			     const double z_len,
			     const double resolution) {
  }
  
}
